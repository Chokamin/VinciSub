# VinciSub · 奇奇字幕 v0.2.0

这一版重点改善字幕断句和默认输出，并减少媒体池中的中间素材。

## 更新内容

- 首次生成优先根据标点、词边界和实测停顿断句，自动接平不超过 0.2 秒的小间隙，保留长停顿。
- 一键优化新增「重新断句」：计算后预览，再同步到原字幕轨。
- 默认清理句中、句尾的句子标点；保留小数、分数、百分数及 USB-C 等技术标识中的必要符号。历史字幕不自动改写。
- 保护数字单位、启用词库里的专名，中英混排按近似显示宽度计算字数上限。
- 改善句中零时长词的有限恢复，减少部分片段生成失败；不可靠时间仍提示校对。
- 成功同步后自动清理媒体池中可确认无引用的插件中间 SRT 素材，保留用户素材、磁盘字幕及恢复记录。
- 保留打开面板时恢复上次任务的功能，没有历史任务时不显示示例字幕。

参考两个频道累计 25 支影片、19,994 条人工字幕，并做短片段画面抽查及模型复测；这不是全片人工观看或识别准确率提升证明。研究方法见仓库 docs/expanded-subtitle-study.md。

## 安装与升级

新安装：下载下方 Source code 源码压缩包，解压到固定目录；先安装 uv 和 FFmpeg，再运行：

```sh
bash scripts/setup.sh
bash scripts/install-resolve.sh
```

旧用户：先关闭 VinciSub 窗口和正在运行的任务，备份原安装目录，更新源码后重新执行上述两条命令。本版新增 jieba 分词依赖，不能只替换脚本入口。保留原目录中的 .vincisub，以保留已下载模型、词库、参考脚本和任务记录；不要用新空目录覆盖它。

重开 DaVinci Resolve，在「工作区 → 脚本 → Utility → VinciSub」启动。「检查更新」仅打开发布页，不会自动安装。

## 验证与限制

130 项单元测试通过；已完成独立达芬奇项目中的原生生成、自动落轨、编辑同步、音频对齐和重新断句回归，三个公开短片段的本地模型生成复测通过。

已实测 Apple Silicon、DaVinci Resolve Studio 21.1、25fps、Python 3.12、Qwen 0.6B / MPS。其他平台、更多帧率及复杂音频路由仍待验证。源音频读取不复现 Fairlight 效果；字幕同步会重建目标轨片段，单条样式请在校对后设置。识别文字及时间仍需人工校对。模型和用户数据不包含在源码包中。
