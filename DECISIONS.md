# 长期决策

## 2026-09-11：项目协作与事实来源

- 决策：代码库是事实源，项目记忆文件是协作源；不以聊天历史保存长期事实。
- 决策：PROJECT_MEMORY.md、DECISIONS.md、HANDOFF.md、CLAUDE.md 均位于项目根目录并纳入 Git，供 Codex 和 Claude Code 跨对话协作。
- 决策：每次工作先读取四份文件；每次修改完成后回写修改记录和交接状态。长期决策及协作规则仅在相应内容变化时更新。
- 决策：每次改动必须编写或更新相关测试，全部测试与验证通过后交付，并创建对应 Git commit。
- 原因：用户要求项目可以跨会话恢复，且每次变更可验证、可追踪、可回滚。
- 影响：后续所有项目修改均遵守 CLAUDE.md 中的协作流程。

## 产品与技术

### 2026-09-11：达芬奇原生字幕脚本

- 产品：VinciSub 为达芬奇脚本，使用原生 UIManager 窗口，不建立网页 UI 或 HTTP 服务。这是用户明确指定的工具形态。
- 识别：本机 Qwen3-ASR，默认 0.6B，配合 Qwen3-ForcedAligner-0.6B 生成实测时间戳；OpenCC 转简体。用户已选择 Qwen3-ASR 本地方案。
- 架构：Resolve 内置解释器仅运行轻量 UI/任务控制；Python 3.12 独立进程加载模型，避免依赖污染并支持取消释放内存。UI 使用原生 Timer 更新状态。
- 数据：任务状态、请求和字幕 JSON 持久化于 `.vincisub/jobs/<uuid>/`；模型位于 `.vincisub/models/`，均不提交 Git。JSON 显式 UTF-8、原子替换；字幕使用秒制时间戳，SRT 使用毫秒和 UTF-8 BOM。
- 初版边界（已由下方决策替代）：手动选择音频识别。
- 验证：macOS Apple Silicon / Resolve Studio 21.1 / Qwen 0.6B MPS 已实测；1.7B、其他操作系统及 Resolve 版本尚未实测。

### 2026-09-11：直接识别所选时间线音轨

- 用户明确拒绝手动导出音频的流程。原生窗口改为选择音轨后直接识别，无文件选择或音频导出步骤。
- 有入点/出点时使用标记范围，否则使用整条时间线；只有单侧时另一侧采用时间线边界。音频标记优先，其次视频标记；出点按包含最后一帧处理。
- 直接通过 Resolve 读取片段源文件、裁剪和通道映射，FFmpeg 将对应音频解码到内存，不创建渲染任务或中间音频文件。
- 字幕时间以整条时间线起点为零，自动加入选定范围偏移；用户无需手动补偿入点。
- 该路径读取源音频，不复现 Fairlight 效果。变速按首尾映射并提示校对；倒放、嵌套/复合与外部同步音频映射暂不支持，不静默读取错误源。

### 2026-09-11：默认跟随多音轨 Solo / Mute

- 默认无需指定音轨：有 Solo 时合并所有未静音的 Solo 音轨，无 Solo 时合并全部未静音音轨；保留显式单轨选项。
- 每次点击生成时重新读取状态，不改变用户的 Solo / Mute 设置；空选择、元数据读取失败不静默回退全部音轨。
- Resolve 21.1 的 GetIsTrackEnabled 只反映 Mute，不反映 Solo。自动模式临时导出 DRT 元数据读取轨道 Flags（Mute=2、Solo=32），通过根时间线句柄匹配容器并核对轨道数量及静音状态；读取后清理。不导出音频、不修改项目数据库。
- 该元数据格式并非稳定公共 API，自动模式限制已验证的 21.1 版本；其他版本提示指定单轨，后续验证后再开放。高级总线路由不在当前支持范围内。

### 2026-09-11：字幕必须写入当前原始时间线

- 用户明确拒绝生成或切换至“字幕版时间线副本”，也不接受手动拖动；完成后的字幕必须位于当前原始时间线的原生字幕轨。
- DRT 副本定位探针只作为历史实验保留，不进入产品工作流。
- 完整 SRT 使用 `mediaPool.AppendToTimeline([mediaPoolItem])` 素材列表重载直接写入；SRT 时间相对于时间线起点，写入后核对全部文字、帧位置和目标轨道。
- 2026-09-11 核查 [SubFix v3.3.1](https://github.com/HooperH/SubFix/blob/v3.3.1/SubFix.lua#L14222-L14238) 的调用方式并在独立项目实测后，移除逐条追加、菜单/检查器定位和 Swift Helper。此前“追加会落在末尾”的结论仅适用于测试过的 clipInfo 字典调用，不能泛化到素材列表重载。禁止对 SRT 传 source startFrame/endFrame。
- 原生窗口保持显示；不抢焦点、不切换页面，API 写入后恢复播放头。不再需要辅助功能或系统自动化权限。
- 每个任务保存落轨凭据防止重复；写入成功后可在插件内校对并同步回本次字幕轨。不得将终端测试或外部 CUA 测试等同于原生生成流程已通过。

- 自动落轨明确停用已有字幕轨再启用本次新轨，避免 SRT 追加忽略目标轨。旧字幕内容保留；失败恢复原轨道启用状态，新轨成功后保持启用。

### 2026-09-11：插件内校对与原字幕轨同步

- 选中/双击字幕后在下方编辑文字和时间，点击「保存并同步」明确提交；保持原字幕轨，不新建字幕轨。
- 原生字幕 SetName 实测失败；向非空字幕轨追加部分 SRT 会偏移，因此同步校验旧内容后刷新整条本次字幕轨。轨道保留，单条片段会重建，单条样式应在校对后设置。
- 用落轨凭据核对 ID/文字/时间，外部编辑冲突或锁轨时拒绝覆盖；写入前备份旧内容，失败恢复并更新恢复后的片段 ID。其他字幕轨与音视频不参与替换。

### 2026-09-11：显式音轨多选

- 音轨通过原生复选列表选择，可同时识别多轨；不勾选时自动读取全部可听音轨。移除界面「跟随 Solo / Mute」提示，但自动模式仍保留其筛选行为。
- 显式选择沿用原单轨语义，读取勾选轨道而不受轨道 Solo / Mute 影响；片段禁用及通道静音仍跳过。刷新同一时间线保留选择，切换时间线清空选择。

### 2026-09-11：不同选区复用字幕轨

- 首次生成新轨；再次生成时，从同项目同时间线的任务凭据中找到最近仍与实际字幕 ID/时间/文字一致的轨道。选区及新字幕均无重叠且未锁轨时复用，否则新建轨道，不覆盖冲突内容。时间首尾相接不视为重叠。
- 非空轨 API 追加实测按旧轨末尾偏移，因此复用采用整轨合并刷新，保持旧字幕文字和时间，单条样式仍在全部识别/校对后设置。
- 凭据记录全轨快照和当前任务 owned_indices，后续校对只替换当前任务字幕；合并失败恢复旧全轨并更新来源任务凭据，不给失败的新任务写成功凭据。

### 2026-09-11：复用以实时轨道内容为准

- 修正旧凭据必须完全一致的限制：新生成只用同项目/时间线历史记录识别 VinciSub 轨名，以实际轨道内容判断覆盖并建立本次刷新快照，保留用户已改文字、时间及增补字幕。校对既有任务仍严格核对凭据，避免覆盖外部修改。
- 优先检查启用轨道，再按轨号检查其余已知轨；单轨冲突不能提前终止搜索。全部候选重叠/锁定才新建。手动改名且无匹配历史轨名的轨道不自动猜测复用。
- 决策记录写入 track-selection.json，实时备份位于本次任务 reuse-source；复用成功启用该轨，失败恢复原轨启用状态。

### 2026-09-11：全部字幕读取与奇奇字幕副标题

- 增加「读取全部字幕」，读取当前时间线全部原生字幕轨，不依赖任务历史、不受入出点/启用状态限制；按时间排序并标注 ST 轨号。跨轨重叠保留各自条目。
- 全部列表选中一条后可编辑并同步其原轨；保留读取时的全轨快照，外部修改时仍拒绝覆盖。读取动作不写时间线；全部模式禁用跨轨 SRT 导出、整体偏移及重复写入按钮。
- 窗口和面板标题统一为 VinciSub · 奇奇字幕，脚本名与内部标识保持 VinciSub。

### 2026-09-11：句尾瞬时时间戳处理

- 句尾零时长词若距离前词末端不超过 0.2 秒，则保留文字并合并，结束时间取原有效跨度与瞬时锚点的最大值；不凭字数推算时长。远距离、全零或无效时间戳仍拒绝，提示与入出点无关。
- 自动合并后的结果提示校对时间，避免把修复视为模型精确逐词对齐。

### 2026-09-11：本地辅助识别词库

- 原生面板提供词库文本、启用开关和保存按钮，最多 100 词/2000 字，每词 64 字；换行及中英文逗号等分隔，去重并保留英文词组空格。
- 本地词库跨时间线共用，保存于 .vincisub/vocabulary.json，不提交用户词汇。生成任务保存当时的词汇快照，每段识别经 Qwen3-ASR context 参数传入；停用保留词条、识别传空 context。
- 这是模型上下文提示，不做识别后强制替换，不保证准确率提升；避免添加大量无关词汇。时间对齐沿用模型实际转写结果。

### 2026-09-11：词库移入二级设置

- 主面板仅在刷新按钮旁保留「词库设置…」入口；词库文本、启用开关和保存操作放入独立原生窗口，减少主面板占用。
- 保存后生效；取消或关闭二级窗口放弃编辑，重新打开从已保存词库载入。取代之前生成/关闭主窗口自动保存词库的行为，生成只读取已保存设置。

### 2026-09-11：模型管理与真实下载进度

- 独立模型管理窗口列出两种 ASR 和共用对齐模型，显示实际缓存大小/状态/路径，提供主动下载、取消、确认删除；主面板只放入口。
- 模型统一使用项目 .vincisub/models/hub 缓存，完整本地缓存无需网络；缺少必需文件则补全。下载根据远程文件元数据与本地已完成/临时文件的字节数报告进度，不用时间模拟百分比，完成校验后才到 100%。
- 轻量小包裹与跳动圆点配合蓝色进度条；下载完成离开下载阶段，不占用日常界面。下载与识别持共享锁，删除持独占锁，防止在用模型被移除。

### 2026-09-11：下载进度使用独立窗口

- 自动下载与手动下载都在开始时弹出独立原生窗口，主面板不再嵌入动态隐藏的进度标签；所有窗口子控件从创建时参与布局，避免隐藏标签恢复后的左上角重叠。
- 窗口显示模型、真实百分比与大小；后台下载或关闭窗口只隐藏窗口，不中断任务，同一下载阶段不反复弹出；取消按钮取消下载，完成后自动关闭并继续识别。

### 2026-09-11：字幕编辑弹窗与精简主面板

- 单击仅选中字幕，双击打开独立编辑窗口，提供文字、开始/结束时间、保存并同步和取消；替代底部常驻编辑区。取消或关闭不保存，输入无效时在窗口内提示，继续沿用原字幕轨同步和冲突保护。
- 移除主面板整体偏移输入；新任务保持自动时间线定位，既有任务已保存的偏移不被清零。主面板保留保存 SRT 和写入字幕轨操作。

### 2026-09-11：参考口播脚本

- 独立「参考脚本」窗口提供粘贴、启用、保存、取消；本机跨时间线保存，最多 6000 字，超限明确拒绝，不静默截断。
- 生成请求保存启用脚本的快照，与词库合并为 Qwen3-ASR context，明确口播可有增删改词与顺序差异。输出继续来自实际识别并按识别文本对齐，不用脚本替换字幕或强制对齐未说出的句子。
- 停用时传空脚本并保留已保存原文；不启用脚本时词库行为保持原状。提示效果仍需校对，不保证准确率提升。

- 参考脚本辅助结果出现无效时间戳时，保留词库并按音频重试该段，成功后记录校对提示；重试仍失败则正常报错，不伪造时间戳。

### 2026-09-11：可预览的一键优化

- 独立窗口可组合选择句尾逗号/句号清理、全部 Unicode 标点清理、短间隙填补；全部标点优先于句尾规则，仅标点字幕不清空。句尾清理保留闭合引号和句中标点。
- 间隙阈值默认 0.5 秒，可调 0–10 秒；只延长前句结束时间，长停顿与末句保持，不改变开始时间。一次处理本次生成的列表，或在全部字幕模式选择一条轨整轨处理，不跨轨填补。
- 预览修改数量和前 8 条变化，应用才同步原轨；取消无写入。整轨任务保留读取时快照，沿用同步冲突保护与恢复机制。

### 2026-09-11：保留文字的音频重新对齐

- 优化窗口提供独立进程 ForcedAligner 重新对齐，先计算再预览应用；使用当前所选音轨和字幕覆盖范围，不改入出点、不导出音频、不重写文字。
- 单条搜索原时间前后各 1 秒，最长窗口 30 秒；句尾保留默认 0.15 秒，可调 0–1 秒，按时间线帧量化并限制在下一条之前。超过单条窗口或无效时间戳、静音、冲突的条目保留原时间，列出序号。
- 取消中止计算、不写入；对齐模型共用现有缓存和使用锁。模型无语义置信度，本轮只用时间戳有效性与冲突检查，明显文字不符或大幅错位需人工校对。

### 2026-09-11：生成后补填脚本优化

- 优化窗口支持补填参考脚本（6000 字上限），带入已有设置但只在本次优化请求快照中使用，不覆盖前期设置。与音频对齐可组合选择。
- 使用本地 Qwen3-ASR 0.6B 和词库逐条重新识别音频，预览后才应用；单独脚本优化保留时间，组合对齐才改时间。音频窗不越过相邻字幕间隙中点，不自动新增/删除字幕。
- 修改脚本/对齐参数使计算缓存失效。计算与应用保留原字幕快照和时间线身份检查，外部切换或编辑仍不能静默覆盖。错误或不可靠结果保留原条目，效果需校对。

### 2026-09-11：统一参考脚本入口

- 移除优化窗口内重复脚本输入框，脚本统一在主面板「参考脚本…」编辑保存。优化明确勾选后使用已保存文本，不依赖生成阶段启用开关；空脚本提示先回主面板填写。
- 每次计算/应用核对当前已保存脚本，内容变化使旧预览失效，必须重新计算；优化过程不覆盖脚本设置。

### 2026-09-11：手动检查 GitHub 更新

- 更新入口位于标题右侧，按需后台读取公开 GitHub 最新正式 Release；以三段版本号比较，不自动检查、安装或覆盖用户文件。新版提供发布页入口。
- 发布源集中在根目录 update-source.json；尚无仓库时留空并明确提示。标签 v1.2.3，本地两个版本声明需一致。
