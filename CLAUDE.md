# 协作规则

适用于本项目中的 Codex 和 Claude Code。

## 开始工作前

每次开始工作，必须先依次阅读项目根目录的以下文件，再分析需求：

1. HANDOFF.md
2. PROJECT_MEMORY.md
3. DECISIONS.md
4. CLAUDE.md

代码库是事实源，项目记忆文件是协作源。不要依赖聊天历史作为长期事实源。新对话必须从这些文件恢复工作，并检查实际代码和 Git 状态；记忆与代码不一致时，以代码为准并修正记忆。

## 修改与验证

- 每次改动后，必须编写或更新相关测试；文档或工作流改动也应有适当的验证。
- 交付前必须运行所有现有测试和与改动相关的验证，确保全部通过。
- 不得将未运行、失败或受阻的检查标记为通过；遇到阻塞时记录原因并说明尚未完成。
- 当前测试命令：`.venv/bin/python -B -m unittest discover -s tests -v`；首次运行先执行 `bash scripts/setup.sh`。修改原生窗口后必须在达芬奇中实测启动、任务完成和相关交互；修改导入功能后使用 `scripts/verify_resolve.py` 的临时项目验证；修改时间线音频读取/范围/音轨选择后使用 `scripts/verify_timeline.py` 的独立项目和原生识别流程验证。不得在用户现有时间线上写入测试素材。

## 完成修改后

- PROJECT_MEMORY.md：追加本次修改记录，包含日期、需求、改动、关键文件、验证结果和剩余问题。
- HANDOFF.md：更新当前状态、下一步任务、已知风险、阻塞点和需要验证的内容。
- DECISIONS.md：仅在形成或调整长期产品、UI、技术、数据结构或工作流决策时更新。
- CLAUDE.md：仅在协作流程本身变化时更新。
- 每次完成改动，必须创建对应的 Git commit，将实现、相关测试和必要的记忆更新一并提交，以便追踪与回滚。
- 提交前检查暂存差异，不混入无关改动或敏感信息；提交后确认 Git 状态，在交付中报告 commit 和验证结果。

## 记忆维护

- 四份文件均保存在项目根目录并纳入 Git，不建立重复的隐藏记忆目录。
- PROJECT_MEMORY.md 记录修改历史；HANDOFF.md 保持最新交接状态；长期决策集中在 DECISIONS.md。
- 日期使用 Asia/Shanghai 时区；不记录密钥、令牌、凭据或敏感日志。
- 不得为了填充记忆而虚构产品需求、技术选型或验证结果。
