# 项目交接

## 当前状态

- 原生达芬奇脚本：工作区 → 脚本 → Utility → VinciSub。已安装的链接直接使用项目代码，关闭旧窗口重新打开即可。
- 默认「自动 · 所有可听音轨」：有 Solo 时识别所有未静音的 Solo 轨道，否则识别全部未静音音轨；保留指定单轨。有入出点就识别该范围，无标记则整条时间线；单侧标记以另一端时间线边界补齐。音频标记优先于视频标记。
- 时间线片段源音频直接解码到内存，无手动文件选择、无交付渲染任务、无中间音频文件。禁用片段/断开通道跳过；显式选择的静音轨道仍可识别。
- 字幕自动保留时间线位置，包括非零时间线起点。支持原生校对、取消、SRT 保存、导入媒体池并检查来源项目/时间线。
- 本次提交说明：`feat: follow timeline solo and mute for multi-track ASR`，确切提交以 Git 日志为准。

## 下一步任务

1. 副本方案已通过简单时间线可行性验证：修改导出 DRT 的字幕 Start 并导入新时间线，可准确定位原生字幕且保留原时间线。先确认用户是否接受新副本，再开发生产路径及复杂时间线完整性验证。
2. 继续解决原生字幕自动落轨：SRT AppendToTimeline 忽略 recordFrame，添加源帧边界会导致应用退出，不要重复该调用。
3. 继续完善速度曲线、嵌套/复合片段和外部同步音频映射；当前限制需如实说明。
4. 验证更多真实口播、长音频及音轨通道配置。
5. 验证更多 Resolve 版本及高级音频路由后再放开自动模式。

## 已知风险

- 自动 Solo / Mute 使用临时 DRT 元数据，限制 Resolve 21.1；读取失败明确报错。总线 Solo / Mute、Solo Safe 未验证。
- 当前识别源音频，不包含 Fairlight 的降噪、效果、音量自动化或总线混音。
- 变速按首尾时间映射并提示校对，非线性速度曲线的局部时序可能偏差。倒放、复合/嵌套、外部同步音频映射明确报错。
- 单次范围最长 30 分钟。首次模型下载需网络；旧版文件任务可能仍留有音频缓存。
- 仅实测 Apple Silicon / Resolve Studio 21.1 / Qwen 0.6B MPS；其他版本、平台及 1.7B 未实测。
- 当前用户人声音轨已只读验证可定位 74 个启用片段，含变速；未对用户整条真实时间线做识别或修改。

## 阻塞点

- 当前音轨和范围识别功能无阻塞。旧的渲染接口失败不再阻塞此工作流。
- 自动将字幕放置到轨道尚未实现：隔离验证中 SRT 追加到末尾、忽略定位参数；含 startFrame/endFrame 的追加曾触发退出，已恢复原项目。生产代码未加入该路径，仍需将媒体池 SRT 拖至时间线起点。

## 需要验证的内容

- `.venv/bin/python -B -m unittest discover -s tests -v`：43 项测试，覆盖范围回退、包含出点、单侧标记、非零起点、通道选择、裁剪、空隙和无音频文件输出等。
- `.venv/bin/python -m scripts.verify_timeline <样本 MP4 绝对路径>`：独立双音轨项目内验证标记、全长回退、多轨混合、静音筛选、默认原生窗口真实识别、字幕范围、SRT 导入与无渲染；结束后恢复原项目并清理测试项目。
- `.venv/bin/python -m scripts.verify_resolve <样本路径>`：独立项目媒体池导入验证。
- 原生窗口更改后关闭重开再验证；更改入出点应自动刷新范围，新建/重命名音轨可点刷新按钮。
- 提交前检查 `git diff --check`，提交后确认工作区干净。

- 多 Solo 已在独立三轨项目通过界面实测：A1/A2 Solo 后仅选择 [1,2]；自动测试覆盖两个 Solo、全部静音、静音与 Solo 组合、锁定轨道、嵌套容器匹配、状态变化与版本拒绝。

- `scripts/probe_subtitle_copy.py <样本 MP4>`：隔离项目验证 DRT 副本原生字幕精确定位，禁止传 source startFrame/endFrame 给 SRT 追加。生产默认导入尚未更改；不宣称支持复杂时间线完整复制。
