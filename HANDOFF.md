# 项目交接

## 当前状态

- VinciSub 原生达芬奇脚本 MVP 已实现；无网页界面或 HTTP 服务。入口 scripts/VinciSub.py，安装后位于 工作区 → 脚本 → Utility → VinciSub。
- 支持选择音视频文件、Qwen3-ASR 本地中文识别、强制时间对齐、简体转换、断句、字幕校对、时间偏移、SRT 保存和导入媒体池。
- 本机已安装 Python 3.12 虚拟环境、依赖、0.6B 识别及时间对齐模型，并安装用户脚本入口。模型和测试样本不提交 Git。
- UI 原生 Timer 驱动状态刷新；模型运行在独立进程。已修复 Resolve 默认 ASCII 编码和入口缺少 __file__ 的兼容问题。
- 本次提交说明：`feat: add native Resolve Chinese subtitle MVP`；确切提交以 Git 日志为准。

## 下一步任务

1. 优先解决自动导出当前时间线音频：当前 MVP 需用户先手动导出音频，再选择文件识别。
2. 调研官方 API 是否支持可靠地将 SRT 直接放到指定字幕轨道；当前仅导入媒体池，再由用户拖入时间线。
3. 用真实口播、音乐背景、多人对话和长音频验证识别及分块边界，再考虑 VAD、说话人和字幕规则优化。

## 已知风险

- 仅实测 Apple Silicon / Resolve Studio 21.1 / Qwen3-ASR 0.6B MPS；1.7B 和其他系统/版本尚未实测。
- 短合成中文样本成功不代表真实素材准确率；仍需人工校对。首次需联网下载模型。
- 任务音频、日志和模型占用本地空间；旧 jobs 需按需清理。
- 移动项目目录会使已安装的符号链接入口失效。

## 阻塞点

- 自动时间线音频导出：实测 AddRenderJob 成功，但 StartRendering 返回 False、JobStatus=Ready。WAV/PCM、MOV/PCM、MP4/AAC 及调用参数对比未得到稳定成功；失败入口和实现未保留在 MVP 中。测试均在独立项目中进行并恢复原项目，不要将该功能记为已完成。
- 当前文件识别 MVP 无阻塞；自动导出属于尚未解决的扩展工作。

## 需要验证的内容

- `.venv/bin/python -B -m unittest discover -s tests -v`：27 项测试，覆盖音频归一化/分块、时间戳、简体标点、任务持久化/取消、原生入口与导入兼容。
- 本机实测：8.7 秒中文语音在原生窗口识别为 3 条字幕；结果自动刷新；校对及 1.5 秒偏移成功持久化；文件对话框和取消任务已检查。
- `.venv/bin/python -m scripts.verify_resolve <样本音视频绝对路径>`：已验证 SRT 导入临时项目媒体池，自动恢复原项目并删除测试项目。
- 修改原生 UI 后重开脚本再验证；不要把单元测试通过等同于宿主交互通过。
- 提交前检查 `git diff --check`，提交后检查工作区干净。
