# 项目记忆

## 最近完成的修改

### 2026-09-11：初始化协作文档

- 需求：建立可跨对话恢复的四份根目录协作文件；每次修改更新记忆、测试并提交 Git commit。
- 改动：创建项目记忆、长期决策、交接状态和共用协作规则；新增基于 Python 标准库的文档验证测试。
- 关键文件：PROJECT_MEMORY.md、DECISIONS.md、HANDOFF.md、CLAUDE.md、tests/test_project_memory.py。
- 验证结果：`python3 -B -m unittest discover -s tests -v` 全部通过（3 项）；`git diff --check` 通过，提交前再检查暂存差异。
- 剩余问题：尚未明确产品需求和技术栈；尚无业务功能可验证。

### 2026-09-11：达芬奇原生中文字幕 MVP

- 需求：用户选择 Qwen3-ASR 本地方案，并明确整个工具必须是达芬奇脚本，没有网页界面。
- 改动：实现原生 UIManager 窗口、独立推理进程、模型缓存、简体转换、对齐时间戳、字幕断句/校对/偏移、SRT 保存及媒体池导入；加入安装脚本、依赖锁定及使用说明。移除先前误做的网页实现，未将网页版本提交。
- 关键文件：scripts/VinciSub.py、vincisub/native_ui.py、jobs.py、worker.py、subtitles.py、resolve.py、README.md、requirements.lock、tests/。
- 验证结果：27 项自动测试；本机 Resolve Studio 21.1 原生启动及中文文件选择、Qwen 0.6B MPS 实际识别 8.7 秒样本为 3 条字幕、原生 Timer 自动展示结果、校对与偏移持久化、任务取消；SRT 导入临时项目实测通过并恢复原项目。提交前执行全套测试和差异检查。
- 剩余问题：自动时间线音频渲染实测失败，当前需先手动导出音频；导入媒体池后需手动拖到字幕轨道。未验证真实长音频、1.7B 或跨系统兼容。

### 2026-09-11：按音轨及入出点直接识别时间线

- 需求：自动读取入点/出点，无标记时识别整条时间线；支持选择音轨；取消手动音频导出和文件选择流程。
- 改动：新增时间线快照与内存解码，按音轨、片段裁剪、通道映射和时间范围读取源音频；窗口显示音轨和实时范围；字幕自动定位到时间线位置；保留项目/时间线身份检查。无需达芬奇交付渲染，不生成音频文件。
- 关键文件：vincisub/timeline.py、native_ui.py、jobs.py、worker.py、tests/test_timeline.py、scripts/verify_timeline.py、README.md。
- 验证结果：35 项自动测试通过；独立双音轨时间线（起始 01:00:00:00）实测无标记全长 8.68 秒、入出点范围 1–5 秒、选择第二音轨只识别其范围内音频，生成字幕位于 2.04–4.84 秒；原生窗口自动展示结果；无渲染任务和中间音频文件。原生 SRT 导入亦通过；原项目恢复、测试项目清理；完整测试和 Git 差异检查通过。
- 剩余问题：源音频路径不复现 Fairlight 混音效果；速度曲线的局部时序需校对；倒放、复合/嵌套、外部同步音频映射暂不支持；字幕导入媒体池后仍需拖入字幕轨道。

### 2026-09-11：默认多音轨识别及 Solo / Mute 筛选

- 需求：不选单轨时识别全部可听轨道，支持同时 Solo 两轨或 Mute 其余轨道；同时继续验证字幕直接落到原生字幕轨。
- 改动：默认新增自动音轨模式，生成时读取当前 Solo / Mute；合并所选轨道的片段并保留各自时间位置。元数据读取失败或无可听轨道时明确报错；保留指定单轨。更新使用说明、回归测试及原生验证脚本。
- 关键文件：vincisub/audibility.py、timeline.py、native_ui.py、tests/test_audibility.py、tests/test_timeline.py、scripts/verify_timeline.py、README.md。
- 验证结果：43 项自动测试通过；Resolve 21.1 独立双音轨项目验证多轨 PCM 混合、静音筛选、默认原生窗口实际 Qwen 识别、入出点对齐、无渲染或音频文件；独立三音轨项目通过界面同时 Solo A1/A2，实际选择仅 [1,2]，排除未静音 A3。测试结束均恢复原项目并清理临时项目。
- 剩余问题：字幕自动落轨未完成。隔离探针中 AddTrack('subtitle') 后 AppendToTimeline(SRT) 能生成原生字幕，但忽略 recordFrame 并放到时间线末尾；加 startFrame/endFrame 导致 Resolve 退出。已恢复事先保存的原项目、清理测试项目，相关调用未纳入生产代码。ImportIntoTimeline(SRT) 返回 False。不得将媒体池导入报告为自动落轨成功。
- 剩余问题：Solo 元数据仅验证 21.1；高级总线、Solo Safe 未验证，仍不复现 Fairlight 效果。

### 2026-09-11：验证原生字幕定位的时间线副本方案

- 需求：解决字幕导入落在时间线末尾的定位问题，保留原生字幕轨。
- 改动：新增隔离验证脚本。在临时项目中复现 SRT 追加位置，再导出 DRT，仅修改副本的字幕 Start 字段，导入新时间线并核对定位及原时间线不变；未改变生产导入流程。
- 关键文件：scripts/probe_subtitle_copy.py、HANDOFF.md。
- 验证结果：Resolve 21.1 / 25fps / 起始 01:00:00:00：原字幕位于 90220–90242 帧，副本准确位于 90003–90025 帧；保留一条视频轨和一条音频轨，原时间线字幕仍在原位置。测试完成恢复原项目、删除临时项目；现有 43 项测试通过。
- 剩余问题：该方案输出新时间线副本，尚需用户确认这一工作流是否可接受；目前仅为可行性探针，不等于生产功能完成。复杂时间线、效果、复合片段、已有字幕及跨版本完整保留尚未验证。不能用未经验证的副本覆盖原时间线。

### 2026-09-11：验证当前时间线的界面辅助字幕定位

- 需求：用户拒绝副本方案，要求验证通过界面操作将字幕准确放入当前原始时间线。
- 改动：新增隔离界面结果验证脚本，核对字幕内容、精确起止帧、时间线身份及音视频片段身份/位置未变。明确记录副本方案已被用户拒绝；生产导入流程未更改。
- 关键文件：scripts/probe_subtitle_ui.py、HANDOFF.md、DECISIONS.md。
- 验证结果：Resolve 21.1 / 25fps / 01:00:00:00 起始，独立项目中先 AddTrack('subtitle') + 不含源帧参数的 SRT AppendToTimeline，再用 CUA 操作检查器的起止时间字段及“下一个”。第一条从 90267–90292 校正到 90050–90075，第二条从 90342–90367 校正到 90125–90150。自动断言 PASS：同一时间线、文字及帧位置准确、音视频片段 ID/起止帧未变。结束恢复已保存原项目、删除测试项目。43 项现有测试通过。
- 剩余问题：快捷键、菜单覆盖和 CUA 拖放尝试未成功插入字幕；不能断言人工拖放不可用。成功路径依赖外部 CUA 选择字幕及编辑原生检查器，目前未接入 VinciSub 独立一键生成。大批量字幕、已有字幕冲突、不同布局/语言、辅助功能权限和中断恢复仍待验证。

### 2026-09-11：接入原时间线自动字幕落轨（验收待授权）

- 需求：把已验证的界面定位接入原生生成流程，识别完成自动写入当前原始时间线字幕轨。
- 改动：新增 placement 进程、独立 Swift 辅助应用、逐条追加/检查器定位/回读、错轨清理、任务凭据和全局锁；窗口生成后自动调用，成功后原生轨道校对。助手通过 LaunchServices 独立启动并申请系统权限；不导出音频、不生成时间线副本。
- 关键文件：vincisub/placement.py、native/ResolveCaptionBridge.swift、native/Helper.entitlements、vincisub/native_ui.py、tests/test_placement.py、scripts/verify_placement.py、scripts/verify_timeline.py、README.md、HANDOFF.md、DECISIONS.md、CLAUDE.md。
- 验证结果：52 项单元测试通过；旧终端助手在独立 25fps / 01:00:00:00 项目准确定位三条字幕，已有 ST1 保留、新字幕 ST2、音视频不变及重复预防均通过；媒体池兼容验证通过。原生真实 Qwen 识别通过，但最初 Resolve 子进程的 Apple Events 被 -1743 拒绝；改独立 app 后进入系统授权/启动调试，完整 verify_timeline 尚未通过。所有测试恢复原项目并清理临时项目，未写入用户原时间线。
- 剩余问题：用户已开启 Helper 权限；修复编译目标和启动后应用仍返回未授权，已请求移除后重新添加授权。最终独立应用还须重新通过 verify_placement 与 verify_timeline，不能把旧终端助手通过当作完整产品验收。非 25fps、英文界面及大批量字幕未实测。

### 2026-09-11：重新授权后完成原生自动落轨实测

- 需求：用户重新添加并开启 Helper 权限后，继续完成自动落轨验证。
- 改动：更新验收状态与使用说明；修复仅启用新字幕轨时仍误投旧轨的问题，写入前明确停用旧轨、启用新轨；增加导入异常的实际新增/错轨内容诊断文件；已有字幕实测增加前置断言，确保测试素材存在。未重新编译助手，避免再次改变本地授权身份。
- 关键文件：vincisub/placement.py、scripts/verify_placement.py、README.md、HANDOFF.md、PROJECT_MEMORY.md。
- 验证结果：授权预检通过；独立 Helper 的三条字幕定位、已有 ST1 保留、新建 ST2、音视频不变和防重复在修复后连续三轮实测通过。原生窗口真实 Qwen 识别生成两条字幕并自动写入同一时间线，准确位于 90051–90105、90109–90121 帧（25fps、起点 90000），无渲染或音频文件。52 项测试通过；临时项目清理并恢复原项目。
- 剩余问题：已有字幕复测捕获新字幕误投 ST1，清理生效；明确切换轨道启用状态后连续复测通过。非 25fps、英文界面、大批量字幕仍待验证。

### 2026-09-11：用素材列表 API 批量直接写入字幕

- 需求：按 SubFix 的直接字幕写入方式替换菜单点击与检查器定位。
- 改动：整份 SRT 经 ImportMedia 后使用 AppendToTimeline(mediaItems) 素材列表重载一次写入；保留目标轨启用、精确帧和文字回读、防重复、失败清理及原轨启用状态恢复。API 写入会移动播放头，完成后恢复其原位置。移除 Swift Helper 源码、构建与启动代码，原生窗口保持显示，不切页面或抢焦点；不再需要辅助功能与自动化权限。每次生成唯一 SRT 文件名避免导入缓存。
- 关键文件：vincisub/placement.py、vincisub/native_ui.py、tests/test_placement.py、scripts/verify_placement.py、native/（已移除）、README.md、HANDOFF.md、DECISIONS.md。
- 验证结果：52 项测试通过；独立项目三条原生字幕准确写入 ST2，原 ST1 字幕及音视频未变，播放头和页面恢复/保持，重复写入不新增。原生生成按钮到真实 Qwen 识别及 API 自动落轨完整通过，两条字幕位于 90051–90105 和 90109–90121 帧，无渲染任务或中间音频文件。原项目已恢复，临时项目已清理。
- 剩余问题：仅实测 Resolve 21.1 / 25fps / Apple Silicon；其他帧率、长任务及复杂时间线仍待验证。旧 Helper 缓存可保留但不会执行，旧授权不再需要。本次只借鉴公开 API 调用方式，未复制第三方插件代码。

### 2026-09-11：插件内字幕编辑并同步原字幕轨

- 需求：生成字幕后直接在插件里修改，并同步到达芬奇字幕轨。
- 改动：启用文字/时间编辑，选中或双击后在下方修改，按钮改为「保存并同步」。新增同步模块，校验来源和旧字幕凭据，刷新本次字幕轨并验证，失败恢复原字幕内容；不创建新轨道。处理锁轨和外部修改冲突。修复选中项字典兼容和已关闭窗口仍被 FindWindow 命中的生命周期问题。
- 关键文件：vincisub/editing.py、vincisub/native_ui.py、vincisub/placement.py、tests/test_editing.py、scripts/verify_placement.py、scripts/verify_timeline.py、README.md、HANDOFF.md、DECISIONS.md。
- 验证结果：58 项单元测试通过；隔离项目同步修改文字和时间、保留其他字幕轨及音视频，模拟追加拒绝后实际恢复旧字幕通过；完整原生生成 → 选中/双击事件 → 编辑框修改 → 保存同步 → 关闭窗口回收通过，字幕轨数量不增加。此前局部字幕追加实测偏移及原生 SetName 失败，已改整轨刷新，不把失败路径报告为成功。结束恢复原项目并清理临时项目。
- 剩余问题：字幕片段会重建，单条样式应在校对完成后设置；外部编辑冲突需在达芬奇继续校对，暂不提供合并冲突或从轨道重新载入。增删字幕行、更多帧率和长任务仍待验证。

### 2026-09-11：音轨多选与提示简化

- 需求：去掉「跟随 Solo / Mute」提示，支持多选音轨。
- 改动：下拉框替换为原生复选列表，支持多轨勾选、同一时间线刷新保留选择；空选择自动识别可听音轨。快照兼容原单轨调用，多选去重并校验轨道存在，避免重复混音。
- 关键文件：vincisub/native_ui.py、vincisub/timeline.py、tests/test_timeline.py、scripts/verify_timeline.py、README.md。
- 验证结果：59 项测试通过；独立 Resolve 项目原生双轨勾选、刷新保留、真实 Qwen 识别、自动落轨及校对同步通过，无渲染或音频输出，结束恢复原项目。首轮命中已打开的旧窗口而未通过，关闭后重新加载新版完整通过。
- 剩余问题：手动勾选读取指定轨道，空选择保留自动 Solo / Mute 逻辑；高级音频路由和长任务仍待验证。

### 2026-09-11：无重叠选区复用已有字幕轨

- 需求：第二次选择不同范围时，若不会覆盖旧字幕，就继续写入同一条字幕轨。
- 改动：新增同项目/时间线凭据查找、选区相交判断；无重叠复用，有冲突或锁轨新建。合并旧字幕并验证全轨，凭据标记当前任务字幕，后续校对保留旧任务内容；失败恢复来源凭据。
- 关键文件：vincisub/placement.py、vincisub/editing.py、tests/test_placement.py、tests/test_editing.py、scripts/verify_placement.py、scripts/verify_timeline.py、README.md。
- 验证结果：62 项单元测试通过。隔离实测旧字幕之间的空档复用、重叠新建、复用后校对保留旧文字/时间、失败恢复及音视频不变通过。原生连续生成 1–5s 与 5–8.68s 两个选区，同轨落字且保留首批内容；结束恢复原项目并清理测试项目。
- 剩余问题：非空轨直接追加仍按轨尾偏移，采用已验证的整轨刷新；单条样式会重建，需完成全部生成和校对后设置。外部修改或缺失凭据的轨道不会自动合并，长任务和其他帧率仍待验证。

### 2026-09-11：修复旧字幕记录失配导致持续新建轨道

- 需求：用户反馈不同选区仍新建字幕轨，要求排查修复。
- 根因：只读发现用户原轨 26 条字幕、旧凭据 15 条且片段 ID 全变；严格历史匹配跳过了可用空档。另有遇到首个冲突轨就退出搜索的问题。
- 改动：新生成按实时轨道内容构建备份并判断重叠，保留手动修改/增补；扫描所有同来源已知 VinciSub 轨，优先启用轨。保存决策诊断与轨名，支持清空后的轨道复用；校对同步继续严格冲突检查。
- 关键文件：vincisub/placement.py、vincisub/editing.py、tests/test_placement.py、tests/test_editing.py、scripts/verify_placement.py、scripts/verify_timeline.py、README.md。
- 验证结果：64 项单元测试通过。用户时间线只读重放选轨选择 ST1、保留 26 条。独立项目模拟全部 ID 改变、手动增补后，空档合并、原生连续识别均复用同轨并保留当前内容；结束恢复原项目。未改动用户已有两条字幕轨。
- 剩余问题：单条样式仍随整轨刷新重建；手动改名无法关联历史的轨道不猜测复用。已产生的多轨本次不自动合并。

### 2026-09-11：读取全部字幕与奇奇字幕标题

- 需求：增加按钮读取之前所有字幕到面板；将 VinciSub 后的副标题改成「奇奇字幕」。
- 改动：新增全轨读取模块与按钮，按时间显示所有轨道字幕并标记 ST；选中后保存到原轨，保留其他轨道和读取时冲突保护。全部模式禁用跨轨导出/偏移；统一窗口及面板副标题。
- 关键文件：vincisub/catalog.py、vincisub/native_ui.py、tests/test_catalog.py、scripts/verify_timeline.py、README.md。
- 验证结果：67 项测试通过；独立项目原生生成、全轨读取（含未启用且跨轨重叠字幕）、编辑后仅更新所属轨和全列表刷新通过。原项目已恢复；更新后的窗口标题实测为「VinciSub · 奇奇字幕」，只读加载用户当前 1 条轨道的全部 32 条字幕。
- 剩余问题：全轨模式暂不提供跨轨 SRT 合并导出；写入仍重建目标轨片段，单条样式在全部校对后设置。

### 2026-09-11：修复句尾零时长词导致识别失败

- 需求：解释已设置入出点却出现零长度时间戳错误，并排查失败。
- 根因：任务正确读取 80.40–89.36s；末尾「了」为 7.60–7.60s，前词结束于 7.44s，超出旧合并阈值。
- 改动：句尾瞬时词在前词末端 0.2s 内时合并文字，并保留模型给出的末端时间；较大间隔仍拒绝猜测。成功结果附校对提示，失败信息明确与入出点设置无关。
- 关键文件：vincisub/subtitles.py、vincisub/worker.py、tests/test_subtitles.py。
- 验证结果：69 项单元测试通过；同一失败请求和源音频在临时任务中先复现，再真实 Qwen 重跑成功，全部字幕位于 80.40–89.36s 范围；未写入用户字幕轨。
- 剩余问题：模型异常时间戳可能仍无法可靠修复，远距离或全零时间戳继续明确报错。

### 2026-09-11：辅助识别词库

- 需求：添加词库，让模型参考人名、品牌和专业词，帮助改善字幕识别。
- 改动：原生面板加入词库编辑、启用/停用、保存；生成和关闭自动保存。解析分隔符、去重、限额，独立任务快照传入 Qwen3-ASR context，不强制替换转写。
- 关键文件：vincisub/vocabulary.py、native_ui.py、jobs.py、worker.py、tests/test_vocabulary.py、tests/test_jobs.py、scripts/verify_timeline.py、README.md。
- 验证结果：74 项测试通过；独立 Resolve 原生保存去重词库、启用真实 Qwen 识别、停用后请求无词汇、自动落轨与全部字幕编辑回归通过；原项目及原词库恢复。本地安装的官方接口源码确认 context 参数。
- 剩余问题：词库为上下文提示，效果依赖素材，不保证命中或准确率提升；当前跨时间线共用，不提供项目级独立词库。

### 2026-09-11：精简主面板，词库移入二级设置

- 需求：主菜单臃肿，希望词库放到二级菜单。
- 改动：主面板只保留右上方「词库设置…」按钮；独立原生设置窗口提供启用、编辑、保存和取消。保存生效，取消/关闭放弃编辑；主窗口关闭时回收二级窗口。
- 关键文件：vincisub/native_ui.py、scripts/verify_timeline.py、README.md。
- 验证结果：74 项单元测试通过；独立 Resolve 项目实际打开设置、保存、重开回读、取消不改词库、停用及真实识别到字幕轨完整通过；原项目和原词库已恢复。
- 剩余问题：无新增阻塞，词库识别效果及已有字幕样式限制保持原状。

### 2026-09-11：模型管理和下载进度

- 需求：独立模型管理页面，支持查看模型大小/下载位置、删除模型；首次下载显示稍有趣味的进度条。
- 改动：新增缓存管理及原生二级窗口，提供状态/大小/路径、下载补全、取消、二次确认删除；自动下载接入真实字节进度和小包裹动画。统一缓存路径，使用/下载与删除互斥保护，检查必需模型文件。
- 关键文件：vincisub/models.py、model_ui.py、worker.py、native_ui.py、tests/test_models.py、scripts/verify_models.py、README.md。
- 验证结果：81 项测试通过。隔离硬链接缓存真实补下载配置文件，原生进度到 100%、删除及用户模型保持完整通过；原生生成→识别→落轨和设置/全部字幕编辑回归通过。只读模型窗口显示 0.6B 实际占用 1.75 GB、完整项目缓存路径。
- 剩余问题：本次未为测试重下整套 GB 权重；网络吞吐与整套首次下载耗时仍取决于实际连接。缓存大小是本地占用，未下载模型显示 0 MB。

### 2026-09-11：从模型管理打开 Finder

- 需求：直接在 Finder 中打开模型下载位置。
- 改动：路径旁添加「在 Finder 中打开」，按当前所选模型打开文件夹；模型未下载时打开下载目录，不创建模型空目录；启动失败在窗口内提示。
- 关键文件：vincisub/model_ui.py、tests/test_models.py、scripts/verify_models.py、README.md。
- 验证结果：83 项单元测试通过，覆盖模型选择、中文空格路径、未下载及打开失败；原生窗口实际点击、隔离缓存下载完成/删除回归通过；Finder 确认打开实际 Qwen3-ASR-0.6B 目录。用户模型保持完整。
- 剩余问题：无新增阻塞；按钮面向当前 macOS 环境。

### 2026-09-11：修复主面板下载进度被隐藏

- 需求：实际下载时只有「模型搬运中」，没有动画与进度条。
- 根因：UIManager 控件初始化 Hidden=True，赋值 Visible=True 不解除 Hidden；真实任务已有百分比与字节数，控件仍隐藏。此前只验证管理窗口进度，漏测主面板可见性。
- 改动：统一更新下载控件的 Text 与 Hidden；下载等待及有进度阶段显示，完成/失败/取消/加载阶段隐藏。
- 关键文件：vincisub/native_ui.py、vincisub/model_ui.py、tests/test_models.py。
- 验证结果：84 项测试通过；独立原生窗口验证隐藏→下载显示→完成隐藏；当前真实 1.7B 下载窗口解除隐藏后，Resolve 原生 UI 实际读到 93%、4.06/4.38 GB 与进度条，随后用户任务自动写入 ST1 并位置校验通过。未中断用户任务或写入额外测试字幕。
- 剩余问题：当前已打开窗口仍运行旧回调，重开脚本后完整采用新版自动显隐逻辑；动画为包裹图标与变化圆点。

### 2026-09-11：独立下载窗口与布局修复

- 需求：下载进度应显示在新窗口，主面板左上角残缺显示不可接受。
- 根因与修正：上次仅核对控件隐藏属性和无障碍文字，漏掉隐藏标签恢复后未正确布局的实际画面。移除主面板进度标签，改用所有子控件从创建时参与布局的独立下载窗口。
- 改动：自动/手动下载开始弹窗，显示模型、包裹图标、动态圆点、百分比与大小；后台下载或关窗仅隐藏，同阶段不重复弹出；取消连接真实取消回调，离开下载阶段隐藏窗口。
- 关键文件：vincisub/model_ui.py、native_ui.py、tests/test_models.py、scripts/verify_models.py、README.md、DECISIONS.md。
- 验证结果：84 项单元测试通过，涵盖弹出次数、后台隐藏、下一阶段重置、取消回调与结束 Hide 调用；真实 Resolve 截图确认独立窗口完整布局无重叠；隔离缓存实际下载完成、弹窗内容更新、删除回归通过。原生窗口 Hide() 不回写 Hidden 属性，测试不再把该属性作为可见性证据。
- 剩余问题：本次布局截图采用固定进度预览；实际下载流程另以隔离缓存补下载验证，未重复下载 GB 权重。

### 2026-09-11：双击字幕弹窗编辑，移除整体偏移

- 需求：移除主面板常驻编辑区，双击字幕在新窗口修改；同时去掉用处不大的整体偏移输入。
- 改动：字幕编辑移入独立原生窗口，提供文字、起止时间、保存并同步和取消；编辑时禁用主面板，取消不保存，无效输入在弹窗提示。普通保存不再收集未提交编辑。移除偏移 UI，保留既有任务的偏移值。
- 关键文件：vincisub/native_ui.py、scripts/verify_timeline.py、README.md、DECISIONS.md。
- 验证结果：84 项单元测试通过；独立 Resolve 项目完整生成/真实识别/自动落轨及双击编辑、取消恢复、无效时间、保存同步原轨、全部字幕模式跨轨保留均通过；合并移除偏移后再次完整通过。实际截图确认编辑窗口布局完整，用户原项目仅只读预览后取消，未修改字幕。
- 剩余问题：原有整轨刷新对单条样式的限制不变；主面板仍保留 SRT 保存和写入按钮。

### 2026-09-11：放大字幕文字输入区

- 需求：编辑窗口里的字幕输入框太小，希望放大。
- 改动：单行 LineEdit 改为多行纯文本 TextEdit，最小高度 120 并随窗口伸展，占用中间空白；错误提示不再占用弹性空间，载入和保存使用 PlainText。
- 关键文件：vincisub/native_ui.py、scripts/verify_timeline.py。
- 验证结果：84 项单元测试通过；独立 Resolve 项目真实生成、编辑框读取、取消、无效时间提示、保存同步及全部字幕编辑完整回归通过；实际截图确认输入框占满中间区域，无重叠。用户项目仅只读打开预览。
- 剩余问题：无新增问题。

### 2026-09-11：参考脚本辅助识别

- 需求：生成字幕时尽量理解和参考口播脚本，同时允许实际音频与脚本部分不同。
- 改动：新增独立参考脚本窗口（启用、纯文本、保存/取消，最多 6000 字）；本地保存与任务快照；脚本和词库共同传 Qwen context，明确以实际音频为准，不用脚本替换字幕。脚本辅助结果无法对齐时保留词库、去掉脚本重试并提示校对。
- 关键文件：vincisub/reference.py、reference_ui.py、native_ui.py、jobs.py、worker.py、tests/test_reference.py、tests/test_jobs.py、scripts/verify_timeline.py、README.md。
- 验证结果：88 项测试通过；独立 Resolve 实测设置保存/取消/停用、请求快照、真实识别及字幕编辑落轨完整通过；参考脚本的额外月球句子未进入字幕（该短选区触发音频重试）。另用完整 8.7 秒音频与不同脚本实测成功，未触发脚本重试，输出保留音频实际的“测试语音识别，并把生成的字幕导入达芬奇”，而非脚本的“看看中文效果怎么样”。原生窗口截图布局通过；原项目及原设置恢复。
- 剩余问题：尚未量化准确率提升，长脚本/1.7B 脚本辅助效果待测；6000 字为产品输入上限，长稿增加推理开销。设置跨时间线共用，换素材需更新或停用。

### 2026-09-11：一键优化字幕

- 需求：独立多选优化窗口，清理句尾逗号/句号或全部标点，填补短间隙且长停顿切断，并提供后续功能建议。
- 改动：新增纯规则优化与原生预览窗口，默认清理句尾逗号/句号，保留句中及闭合引号；全部标点优先，纯标点字幕不清空。间隙阈值默认 0.5 秒、可调 0–10 秒，末句和长间隙不延长。处理本次生成列表或全部模式所选单轨；预览数量/前 8 条变化，应用后同步原轨，取消不写入。
- 关键文件：vincisub/optimize.py、optimize_ui.py、catalog.py、native_ui.py、tests/test_optimize.py、scripts/verify_timeline.py、README.md。
- 验证结果：92 项测试通过（引号、句中标点、英文句号、全部标点、空字幕保护、阈值边界、输入不变及幂等）；独立 Resolve 真实生成/编辑/优化预览取消/原轨同步及其他轨 ID 和内容保留完整通过；实际截图确认原生窗口布局。测试初期在前一同步未结束就打开优化，被忙碌检查拦截，已修正等待时序并通过。用户原项目只读展示预览，未应用。
- 剩余问题：现有整轨重建对单条样式的限制不变；多轨需逐轨选择优化；多余空格清理、阅读速度提醒作为建议，尚未实现。

### 2026-09-11：音频重新对齐与生成后参考脚本优化

- 需求：优化中重新对齐音频、避免提前结束；已有字幕生成后也可补填参考脚本进一步优化。
- 改动：新增独立计算进程，现有音轨内存读取与 ForcedAligner 逐条对齐；原时间前后各 1 秒搜索、默认 0.15 秒句尾保留、帧量化和冲突保护。新增脚本输入和勾选项，本地 Qwen 0.6B 结合脚本/词库重新识别；单选脚本只改文字，组合对齐才改时间。先计算后预览应用，脚本/参数变化使缓存失效；计算可取消。应用保留来源快照并检查时间线身份。
- 关键文件：vincisub/realign.py、optimize_ui.py、native_ui.py、timeline.py、tests/test_realign.py、scripts/verify_timeline.py、scripts/verify_script_optimization.py、README.md。
- 验证结果：97 项单元测试通过；独立 Resolve 生成、编辑、优化、对齐计算预览和原轨保护完整回归通过。与音频不符的测试文字保留时间并列出需校对；完整 8.68 秒真实口播文本独立对齐将 0.5–8.0s 修正到 0–8.68s，无失败条目且文字不变。原生脚本优化实测把“中午字幕工具”改成实际的“中文字幕工具”，计算前后原时间 0.5–8.0s 完全一致，预览前不应用；截图确认新窗口布局。用户项目仅只读展示。
- 剩余问题：对齐仅在前后 1 秒内搜索，单条窗口超 30 秒保留；模型无语义置信度，明显文字不符仍需校对。脚本优化按原条目处理不新增/删除，可能重新改动已校对文本，需检查预览；生产模型使用固定 0.6B。

### 2026-09-11：优化复用主面板参考脚本

- 需求：移除一键优化窗口中重复的脚本输入框。
- 改动：优化直接读取主面板「参考脚本…」保存的文本，保留独立优化勾选项；未填写时提示回主面板保存。生成阶段停用脚本不影响明确勾选的优化；保存内容变化使旧预览失效。缩小窗口并保留预览空间。
- 关键文件：vincisub/optimize_ui.py、tests/test_realign.py、scripts/verify_script_optimization.py、README.md。
- 验证结果：97 项单元测试通过；原生模型流程验证读取已保存且生成阶段停用的脚本，纠正文字并保持 0.5–8.0s 时间不变；截图确认重复输入框消失且布局完整。验证恢复原脚本设置，未写入用户字幕轨。
- 剩余问题：参考脚本优化仍需人工检查预览；现有模型与同步限制不变。

### 2026-09-11：检查更新入口

- 需求：增加检查更新按钮；用户计划以后在 GitHub 发布，尚无仓库地址。
- 改动：标题右侧新增按钮和独立原生窗口，后台检查正式 Release，显示当前版本、说明、重试和新版发布页入口。新增 update-source.json 预留仓库；空配置明确提示，不伪报最新版本；不自动安装。
- 关键文件：vincisub/updates.py、update_ui.py、native_ui.py、update-source.json、tests/test_updates.py、scripts/verify_updates.py、README.md。
- 验证结果：102 项单元测试通过；原生新版本模拟、重试、链接回调、关闭及真实主按钮空配置流程通过；截图确认主面板和更新窗口布局。未写入时间线。
- 剩余问题：等待实际 GitHub 仓库与首个正式 Release 后配置发布源并验证真实联网结果；本轮新版响应为模拟数据。

### 2026-09-11：准备 GitHub 首次公开发布

- 需求：用户明确要求发布到 GitHub 并选择公开仓库。
- 改动：创建 Chokamin/VinciSub 公开仓库，关联 origin，配置更新源，补充下载入口与 v0.1.0 发布说明，更新发布源测试与原生验收脚本。
- 关键文件：update-source.json、README.md、RELEASE_NOTES.md、tests/test_updates.py、scripts/verify_updates.py。
- 验证结果：103 项测试通过，差异检查通过；全部 Git 历史的缓存路径和常见凭据模式检查通过；模型、素材、虚拟环境均未跟踪。
- 发布结果：main 已推送，v0.1.0 正式 Release 已公开；终端与达芬奇真实联网均返回最新正式版 v0.1.0。原生验收初次过早读取未处理事件的空状态，修正等待时序后全流程通过。
- 剩余问题：更高版本升级提示已通过模拟验证；跨平台范围仍以 README 为准。

### 2026-09-12：自动清理媒体池字幕中间素材

- 需求：无需按钮，成功写入/同步后自动清理 edit/placement 素材，并检查历史积累。
- 改动：写入进程成功后按任务路径、项目 ID、成功记录确认归属，遍历所有时间线排除引用素材；递归媒体池清理匹配 UUID 文件名的 SRT。失败/未完成历史任务、普通用户素材保留；磁盘文件和恢复记录不删除，清理失败不影响字幕同步，记录 cleanup.json。
- 关键文件：vincisub/cleanup.py、placement.py、tests/test_cleanup.py、scripts/verify_cleanup.py、verify_timeline.py、README.md。
- 验证结果：106 项测试通过；Resolve 21.1 独立项目删除中间素材后字幕 ID/内容/时间及保存重开不变，失败备份/用户素材/磁盘文件保留；完整落轨回归及原生 ASR/编辑/优化同步回归通过，原生清理记录实际移除素材。原用户项目恢复；当前项目未找到可确认的成功任务，因此未执行历史删除，等待下次成功同步。
- 剩余问题：缺失任务凭据、失败任务和无法确认归属的历史素材保留；仅支持已验证的 Resolve 21.1。GitHub v0.1.0 发布包尚不含本次修改。

### 2026-09-12：标点优先断句与可预览重新断句

- 需求：首次识别优先按标点和停顿断句，优化中也能重新整理已有长字幕，断句先于去标点。
- 改动：逗号/分号结合 4 个有效字符或 1 秒时长分句，短引导语并入下句；句末标点支持闭合引号，上限切分优先退回已有标点。保留对齐文字中的标点/引号，拒绝丢词。优化新增重新断句和字数设置，以本地 ForcedAligner 测量词级时间，保留整体边界、先预览再同步；支持条数变化和 owned_indices 更新，其他字幕保留。不可靠条目保留，参数变化使预览失效。
- 关键文件：subtitles.py、worker.py、realign.py、optimize_ui.py、native_ui.py、editing.py、tests/test_segmentation.py、test_editing.py、test_realign.py、scripts/verify_script_optimization.py、verify_placement.py、verify_timeline.py、README.md。
- 验证结果：113 项测试通过；真实口播独立原生重新断句保留全部文字与整体时间；独立落轨验证一条变两条且保留原轨/其他内容；完整原生 ASR、校对、优化及新增长句重新断句→去句尾标点→应用原轨流程通过；截图确认布局。初期不完整测试口播被对齐保护拒绝，改用完整文本后通过；原生旧文案断言及生成前选区时序已更新。用户项目仅只读展示。
- 剩余问题：已有字幕逐条拆分，不跨条合并；已去掉的标点不自动恢复，按停顿和长度拆分。超过 30 秒搜索窗或不可靠对齐保留原条目；模型精度仍需校对。尚未发布新版。

## 关键文件

- PROJECT_MEMORY.md、DECISIONS.md、HANDOFF.md、CLAUDE.md：跨对话协作入口。
- scripts/VinciSub.py、scripts/install-resolve.sh：原生脚本启动与安装。
- vincisub/native_ui.py、jobs.py：原生窗口、任务状态和结果保存。
- vincisub/worker.py、timeline.py、audio.py、subtitles.py：本地识别、时间线音轨读取及字幕规则。
- vincisub/resolve.py、scripts/verify_resolve.py：媒体池导入及隔离实测。
- tests/：文档、入口、音频、字幕、任务和 Resolve 适配测试。
- README.md：安装、使用方法、当前边界和验证命令。

## 短期问题

- 直接时间线识别已替代手动导出，当前不再依赖曾失败的渲染接口。
- 后续验证真实长音频、变速及更多音频映射；直接 API 完整原生流程已通过，继续验证更多帧率、界面语言及长任务表现。

### 2026-09-12：参考壹加壹的字幕节奏改进断句

- 需求：研究频道至少前四支影片的字幕与音频关系，样本不足则增加，再改善字幕呈现。
- 研究范围：按列表前四支公开影片（中间两支会员专享跳过），完整统计 2,411 条人工字幕；分别抽查约 8–10 秒画面与声音，另检查静帧。声学关系由本地 ForcedAligner 测量，不冒充人工逐秒观看/听校。记录来源、时间段、统计口径和限制；媒体与全文字幕仅在忽略缓存中，不发布。
- 改动：加入固定版离线中文词典分词保护；词间停顿可辅助无标点断句，接近上限优先退回标点/实测停顿；首次生成及重新断句内部接平最多 0.2 秒间隙，保留开始、长停顿和末条结束。保护千位数字逗号。默认字数、界面选项不变。
- 关键文件：vincisub/subtitles.py、worker.py、realign.py、pyproject.toml、requirements.lock、tests/test_segmentation.py、docs/subtitle-rhythm-study.md、README.md。
- 验证结果：119 项测试通过，git diff --check 通过；独立 Resolve 原生生成、真实模型、优化预览、重新断句、原轨同步完整回归通过，恢复用户项目「2026-08-30_小蚕宣传片 / 正片」。初版纯停顿规则实测误切“中文字／幕工具”，补词边界保护后回归生成“欢迎使用中文字幕工具。”完整一条。新增回归覆盖该问题。锁文件仅增加 jieba 0.42.1，现有其他依赖版本不变。
- 剩余问题：研究是完整字幕统计加片段抽查，非四部完整人工观看，不宣称全面复现该频道或准确率提升。分词词典不能保证专名/语义边界，单个不可分词可超过字数上限；镜头和说话人变化未接入。模型估计的停顿仍需校对；具体阈值为保守产品选择，不是频道公开参数。尚未发布新版。

### 2026-09-12：新开脚本不恢复旧字幕

- 需求：刚打开窗口时不要显示上次任务的字幕和完成提示。
- 改动：Jobs 初始化为无当前任务，不再读取全局 native-latest.json 自动恢复；生成或明确读取全部字幕后才显示内容。保留历史任务、凭据和原时间线字幕，坏的旧指针也不影响启动。
- 关键文件：vincisub/jobs.py、tests/test_jobs.py、scripts/verify_timeline.py、README.md。
- 验证结果：119 项测试通过；新增/更新完成、运行、错误、取消状态及坏指针回归，确认旧文件不变；独立 Resolve 原生完整流程通过，包含等待定时器后启动空列表/就绪状态、真实生成显示、读取全部字幕、编辑优化及同步。原项目已恢复。
- 剩余问题：无本次新增阻塞；旧任务不再自动恢复，已有时间线字幕通过「读取全部字幕」进入面板。
